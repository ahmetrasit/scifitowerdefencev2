/**
 * PathfindingSystem - A* pathfinding (placeholder for now)
 */
class PathfindingSystem {
    // Placeholder - will implement A* later
    static findPath(start, end) {
        // For now, just return direct path
        return [game.grid.gridToWorld(end)];
    }
}
